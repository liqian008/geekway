<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bruce.geekway.dao.mapper.WxProductMapper">
	
	<!-- 查询指定tagId的product列表 -->
	<select id="queryProductsByTagId" resultMap="BaseResultMap">
		select <include refid="Product_Tag_Base_Column_List" /> from wx_product wp, wx_product_tag_relation wptr where wp.id = wptr.product_id and wptr.product_tag_id=#{tagId,jdbcType=INTEGER} order by wptr.top_time desc;
	</select>
	
	
	<!-- limit查询指定tagId的product列表 -->
	<select id="queryProductsByTagIdLimit" resultMap="BaseResultMap">
		select <include refid="Product_Tag_Base_Column_List" /> from wx_product wp, wx_product_tag_relation wptr where wp.id = wptr.product_id and wptr.product_tag_id=#{tagId,jdbcType=INTEGER} order by wptr.top_time desc limit #{limit};
	</select> 
	 
	
	<!-- 查询不在tagId中的product列表，供添加 -->
	<select id="queryProductsOutTagId" resultMap="BaseResultMap" parameterType="java.lang.Integer">
		select <include refid="Product_Tag_Base_Column_List" /> from wx_product wp where wp.id not in (select product_id from wx_product_tag_relation wptr where wptr.product_tag_id=#{tagId,jdbcType=INTEGER}) order by wp.create_time desc;
	</select> 
	
  
  <sql id="Product_Tag_Base_Column_List">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon May 12 11:41:38 CST 2014.
    -->
    wp.*
  </sql>
	
</mapper>