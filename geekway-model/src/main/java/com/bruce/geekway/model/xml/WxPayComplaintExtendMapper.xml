<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bruce.geekway.dao.mapper.WxPayComplaintMapper">
  
  
  
  
	<!-- 查询分页 -->
	<select id="groupSelectByExample" parameterType="com.bruce.geekway.model.WxPayComplaintCriteria" resultMap="BaseResultMap">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Tue Sep 16 18:22:03 CST 2014.
    -->
    select
    <include refid="Base_Column_List" />
    from (select <include refid="Base_Column_List" /> from wx_pay_complaint 
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    order by id desc) sub 
    
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
    group by trans_id
    <if test="limitRows != null">
      limit 
      <if test="limitOffset != null">
        #{limitOffset}, 
      </if>
      #{limitRows}
    </if>
  </select>
  
  
  <select id="groupCountByExample" parameterType="com.bruce.geekway.model.WxPayComplaintCriteria" resultType="java.lang.Integer">
    select count(trans_id) from (
     select trans_id from wx_pay_complaint
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
     group by trans_id
     ) sub
  </select>
  
</mapper>